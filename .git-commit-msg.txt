Multiple UI fixes and additions

- Remove decorative SVG bottle icon from header/footer logo (text-only)
- Modal: no scrollbar (fits within 92vh), gender badge no longer
  overlaps the close button, radar chart resized so axis labels no
  longer get clipped
- Product cards: price row now pinned to the bottom of every card
  regardless of description length (flex layout)
- Products page: quick gender filter (Todos/Mujer/Hombre/Unisex) on
  the left, Descargar Catalogo button on the right, same toolbar row
- Contact section: rebuilt as a two-column row (heading+subtext on the
  left, "Hablemos" info on the right); removed the contact form
- New catalogo.html: printable full catalog with a "Descargar /
  Imprimir PDF" button (browser print-to-PDF, no extra dependencies)
- Hero: "Explorar Coleccion" renamed to "Ver Productos" and now links
  to productos.html; both hero buttons share the same size
- "Ver todo el catalogo" button renamed to "Ver todos los productos"

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
